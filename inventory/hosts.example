# =============================================================================
# Ansible Inventory - Example
# =============================================================================
# Copy this to 'hosts' and edit with your actual servers
#
# cp hosts.example hosts
# nano hosts
# =============================================================================

# -----------------------------------------------------------------------------
# Web Servers
# -----------------------------------------------------------------------------
[webservers]
web1 ansible_host=192.168.1.10
web2 ansible_host=192.168.1.11
# web3 ansible_host=10.0.0.50

# -----------------------------------------------------------------------------
# Database Servers
# -----------------------------------------------------------------------------
[databases]
db1 ansible_host=192.168.1.20
# db2 ansible_host=192.168.1.21

# -----------------------------------------------------------------------------
# All Production Servers
# -----------------------------------------------------------------------------
[production:children]
webservers
databases

# -----------------------------------------------------------------------------
# Development/Test Servers (run here first)
# -----------------------------------------------------------------------------
[dev]
testbox ansible_host=192.168.1.100

# -----------------------------------------------------------------------------
# Variables for all hosts
# -----------------------------------------------------------------------------
[all:vars]
# SSH user for Ansible to connect as
ansible_user=admin

# Use sudo for privilege escalation
ansible_become=yes
ansible_become_method=sudo

# Python interpreter path (uncomment if needed)
# ansible_python_interpreter=/usr/bin/python3

# -----------------------------------------------------------------------------
# Group-specific variables
# -----------------------------------------------------------------------------
[webservers:vars]
# Web servers might need different firewall rules
firewall_http_enabled=yes

[databases:vars]
# Database servers might need postgres port open
firewall_postgres_enabled=yes
